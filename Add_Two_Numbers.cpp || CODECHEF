#include <bits/stdc++.h> 

using namespace std;

int main() {
	// Read the number of test cases.
	int T;
	cin >> T;
	for (int i = 1; i <= T; i++) {
		// Read the input a, b
		int a, b;
		cin >> a >> b;

		// Compute the ans.
		int ans = a + b;
		cout << ans << "\n";
	}

	return 0;
}
